# --- Cấu hình Flask (Backend) ---
# Chuyển từ 'development' sang 'production' khi chạy trên domain
FLASK_ENV=production
FLASK_APP=app.py

# Trên server, thường không cần chỉ định PORT nếu dùng Nginx/Apache, 
# nhưng nếu bạn chạy trực tiếp Flask, hãy dùng cổng mà server mở (mặc định thường là 80 hoặc 443)
PORT=80

# --- Cấu hình Bảo mật & CORS ---

ALLOWED_ORIGINS=https://360web-snowy.vercel.app/
SECRET_KEY=mot_chuoi_ky_tu_ngau_nhien_va_bao_mat_cua_ban

# --- Cấu hình Dịch vụ Google (TTS) ---
# Đảm bảo đường dẫn này chính xác trên thư mục server
GOOGLE_APPLICATION_CREDENTIALS=keys/google-tts-key.json